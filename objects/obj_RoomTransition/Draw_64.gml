// par_RoomTransition Draw Event
/*
if (useFadeTransition && transition_active) {
    draw_set_alpha(fade_alpha);
    draw_rectangle_color(0, 0, display_get_width(), display_get_height(), c_black, c_black, c_black, c_black, false);
    draw_set_alpha(1);
}
